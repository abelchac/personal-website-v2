import type { Metadata } from "next";
import { Inter } from "next/font/google";
import Link from "next/link";
import RAM_Project from "./page";
import NbaAwardsProject from "./page";
import BicycleGANProject from "./page";
import KdTreesProject from "./page";
import LyricGenProject from "./page";
import AdderProject from "./page";
const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function AdderProjectLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    
      <section>
        <AdderProject></AdderProject>
      </section>
      

  );
}
